# Cached private CA bundle, mounted into the container at
# /certs. Persisted on the host so container restarts do not need
# connectivity to the private CA endpoint. Never commit the bundle itself.
*.pem
*.crt
